# ─── DATOS PERSONALES / SECRETS (CRÍTICO) ────────────────────────────
# Cualquier .md en context/ se considera PRIVADO por defecto. Solo se
# permiten los *.template.md (versiones genéricas públicas) y los
# explícitamente marcados como exempt más abajo.
context/*.md
!context/*.template.md
!context/REQUISITOS-THEMEFOREST.md

# Memoria de Claude Code / agents — local-only, nunca al repo.
.claude/
CLAUDE.md
AGENTS.md
MEMORY.md
memory/
.cursor/
.windsurf/

# Notas internas pre-publicación. Si decides publicarlas como
# transparencia histórica, quita esta línea.
ANTES-DE-PUBLICAR.md

# Configs / state runtime con keys
~/.config/themeforge/keys.json
**/keys.json
**/.env
**/.env.*
!**/.env.example
**/credentials.json
**/.credentials.json
# licensing.json local-only por si pones la versión real (con tu URL
# de panel y tu github_org) en el repo dir por error. La versión real
# va a ~/.config/themeforge/licensing.json (fuera del repo).
**/licensing.json
!licensing_config.py

# Builds privados
builds/

# PyInstaller / build artefactos
build/
dist/
*.spec
icon.iconset/
assets/themeforge.icns
# AppImage build outputs
*.AppImage
ThemeForge.AppDir/
appimagetool-*.AppImage
pkgroot/
# .deb / .rpm outputs (built locally)
*.deb
*.rpm
# AUR PKGBUILD scratch dirs
packaging/aur/*/pkg/
packaging/aur/*/src/
packaging/aur/*/*.pkg.tar.zst
packaging/aur/*/*.tar.gz
out/


# ─── Python ─────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ─── Node (terminal embebida) ───────────────────────────────────────
terminal/node_modules/
**/node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── OS / Editor ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# ─── Temporales del propio ThemeForge ──────────────────────────────
*.tmp
*.bak
tmp/

# Node + git portables bundleados en el build de Windows (CI)
vendor/

# Sistema PRIVADO de búsqueda de locales/inmuebles (no se publica)
inmo_scraper.py
inmo_panel.py

# Plantillas privadas del generador de webs (IP de la agencia — NO publicar)
templates/booking-saas/
templates/pro-landing/
templates/restaurant-saas/
templates/shop-lite/

# Herramientas PRIVADAS de agencia (no se publican en el repo OSS).
# Las pestañas se auto-ocultan en el cliente si su motor no está presente
# (web_shell expone __TF_DATA__.features; ver guards en los temas WebUI).
# --- Leads / Captación (Google Places) ---
lead_gen.py
leads_panel.py
dossier_dialog.py
spain_geo.py
webui/neotokyo/app/screen-leads.jsx
webui/kawaii/themeforge-kawaii-private.jsx
webui/matrix/themeforge-matrix-private.jsx
# --- Generador de webs "x100" (Fase 2 del lead-gen) ---
web_generator.py
generator_dialog.py
webui/neotokyo/app/screen-generator.jsx
# --- Gateway móvil: métodos privados (Leads/Generador) ---
api_gateway_private.py
# --- WebUI: slots privados del puente (Leads/Generador/Scraper) ---
web_shell_private.py
# --- App nativo: integraciones privadas (pestañas Leads/Catálogo + Galería) ---
themeforge_private.py
# --- Scraper de catálogo de productos ---
product_scraper.py
catalog_panel.py
webui/neotokyo/app/screen-catalog.jsx
webui/mobile/app-private.jsx

# Móvil: dependencias y artefactos de build de Capacitor/Android (sí va el fuente)
mobile/node_modules/
mobile/android/.gradle/
mobile/android/build/
mobile/android/app/build/
mobile/android/local.properties
mobile/www/
# Secretos FCM/firma de Android — NUNCA al repo público
mobile/android/app/google-services.json
google-services.json
*.keystore
*.jks
